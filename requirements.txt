# Python requirements for Zeris integrations
# Note: there is no full Python SDK in this repo yet. These
# packages provide a sensible base for writing integration
# scripts, examples, or a future Python SDK. They are chosen
# to be compatible with Windows and general server setups.

# Solana client and crypto
solana>=0.11.0
pynacl>=1.4.0

# HTTP clients (sync + async)
requests>=2.28.2
httpx>=0.24.1
aiohttp>=3.8.4

# Crypto helpers
pycryptodome>=3.18.0

# Utility
python-dotenv>=1.0.0
base58>=2.1.1
msgpack>=1.0.4