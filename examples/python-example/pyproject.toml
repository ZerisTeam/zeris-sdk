[project]
name = "zeris-python-example"
version = "0.1.0"
description = "Minimal Python example for Zeris SDK integrations"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
